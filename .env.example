# Required in production (Vercel): the password you type to open the app.
APP_PASSWORD=
# Set automatically by Vercel's Neon Postgres integration. Leave empty locally
# to use the built-in local database (stored in .data/).
DATABASE_URL=
